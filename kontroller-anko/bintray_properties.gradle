if(!project.hasProperty('BINTRAY_PACKAGE')) project.ext.BINTRAY_PACKAGE = 'kontroller-anko'
if(!project.hasProperty('ARTIFACT_VERSION')) project.ext.ARTIFACT_VERSION = '0.0.1'
if(!project.hasProperty('DESCRIPTION')) project.ext.DESCRIPTION = 'A Controller library for Android with an anko extension'
